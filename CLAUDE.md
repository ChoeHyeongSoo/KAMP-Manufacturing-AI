# KAMP 제6회 K-인공지능 제조데이터 분석 경진대회 — 프로젝트 규칙

## Git / 커밋 규칙 (필수, 예외 없음)

- **AI 저작자 표시 금지.** 커밋 메시지·PR 설명·코드 주석 어디에도 `Co-Authored-By`, `Generated with`, `🤖` 등 AI 도구를 가리키는 문자열을 절대 포함하지 않는다. 블라인드 평가 대회이므로 작성 도구·소속·이름 등 식별 정보를 전부 배제해야 하며, 이 규칙은 다른 어떤 기본 첨부 규칙(도구의 기본 attribution 설정 등)보다 우선한다.
- **커밋 메시지 제목은 명사형으로 종결한다** (동사 활용형 금지).
  - 좋음: `사출성형 데이터 품질 진단 리포트 추가`, `브랜치 네이밍 규칙에 이니셜 추가`
  - 나쁨: `추가했습니다`, `추가함`, `리포트를 추가합니다`
  - 형식: `<type>(<scope>): <명사형 설명>` — type: `feat`/`fix`/`docs`/`chore`/`refactor`, scope: 주제 폴더명(`01_injection`, `03_press_pump`, `04_xray`) 또는 영역명(`common`, `docs`)
- **분석/검증 커밋은 본문에 수치를 포함한다.** 데이터 진단·EDA·모델 평가처럼 결과가 나오는 커밋은 메시지 본문(bullet)에 그 커밋에서 확인한 구체적 수치(행 수, 비율, 지표값 등)를 적는다. 예:

  ```
  feat(01_injection): 데이터 품질 진단 노트북·리포트 추가

  - labeled cn7 1,211행 / rg3 1,182행, unlabeled 각 약 35,000행
  - 라벨 충돌 쌍: cn7 11/17건, rg3 25/25건
  - unlabeled 비가동 고정값 블록: cn7 51.5%, rg3 36.6%
  ```

- **브랜치 네이밍**: `<type>/<이니셜>-<작업내용>` — `main`(제출 기준, 직접 커밋 금지)에서 분기해 작업 후 PR.
  - type: `feat/`(분석·모델·노트북 등 산출물 추가), `fix/`(버그·수치 오류 수정), `docs/`(README·리포트·회의록·발표자료), `test/`(검증 스크립트·재현성 점검), `chore/`(환경·설정·폴더 구조), `refactor/`(동작 변경 없는 정리)
  - 이니셜: 성+이름 **세 글자 대문자**(예: CHS). 두 글자(HS)는 팀원 간 겹칠 수 있어 쓰지 않는다. 팀원별 이니셜은 아래 표 참고.
  - 작업내용: 소문자·하이픈, 주제 작업이면 주제 폴더명 접두(`01-injection`, `03-press-pump`, `04-xray`)
  - 예: `feat/CHS-01-injection-data-quality`, `docs/CHS-topic-selection-meeting`, `chore/CHS-repo-structure`, `test/CHS-run-all-reproducibility`
  - 팀원 이니셜 (합류 시 추가):

    | 이니셜 | 비고 |
    |---|---|
    | CHS | 레포 초기 구성, ①·③ 품질 진단 |
    | (추가) | |
    | (추가) | |
- **PR**: 제목은 커밋 형식(`<type>(<scope>): <명사형 설명>`)과 동일, 본문은 `.github/pull_request_template.md`(작업 단계·작업 내용·주요 결과·다음 작업)를 따른다.
  - 병합은 merge commit 또는 rebase merge (squash 금지 — 커밋별 수치 기록 보존).
- 제출물(노트북·리포트·발표자료)에는 소속·이름·로고 등 식별 정보를 넣지 않는다.
- `.claude/`는 개인 작업 로그·로컬 설정 영역이라 git에서 제외한다(`.gitignore`). 팀과 공유할 규칙·기록은 이 파일(`CLAUDE.md`, 루트)이나 `docs/`에 남긴다.

## 심사 기준 (100점) — 모든 작업은 아래 배점 중 무엇에 기여하는지 염두에 두고 진행한다

| 문항 | 배점 | 방향 |
|---|---|---|
| 1. 데이터 이해 및 진단 | 15 | 결측·이상치·라벨 신뢰도·누수 가능성까지 서술 |
| 2. AI 예측모델 개발 | 40 | 베이스라인 포함 **2개 이상 모델 비교** 필수, 배점이 가장 크므로 우선순위 최상위 |
| 3. 영향요인 및 오류분석 | 15 | FN/FP가 집중되는 조건을 구체적으로 짚기 |
| 4. 현장 활용방안 | 10 | 예측을 실제 공정에 어떻게 적용할지 (경보 임계값, 조치 프로세스 등) |
| 5. 창의성·차별성 | 10 | 도메인 지식과 결합한 피처, 새로운 관점의 분석 |
| 6. 코드 및 재현성 | 10 | 노트북 실행 결과 포함 커밋, `requirements.txt` 고정, 경로 하드코딩 금지 |

- 작업/리포트 작성 시 위 표의 몇 번 항목에 해당하는지 스스로 점검한다. 2번(40점)이 최우선이지만 3·4·5번(합 35점)을 놓치지 않도록 "발견 → 시사점 → 활용 아이디어" 흐름을 리포트에 남긴다.
- 진단·분석 중 나온 **추가 인사이트/아이디어**(피처 후보, 이상탐지 전략, 현장 적용 시나리오 등)는 각 주제 `reports/`에 명시적으로 기록해 다음 단계(모델링) 작업자가 바로 활용하게 한다.

## 현재 작업 도메인 (주제 선정 전 — **주제 확정 시 이 섹션 갱신**)

- 대회 기간: 2026-09-21 ~ 2026-10-08(목) 23:59
- 후보 주제: ① 사출성형 품질불량 예측 / ③ 프레스 유압펌프 이상 조기탐지 / ④ X-ray 이물질 탐지 (②⑤ 후보 제외)
- 팀: 3인, 브랜치 단위로 주제/작업 분담 (이니셜 접두사로 구분)
- 진행 상태 (상세는 각 `topics/<주제>/reports/01_data_quality.md` 참고):
  - **① 사출성형** — 품질 진단 완료. labeled 데이터가 정확히 2회씩 쌍으로 존재, 라벨 충돌 비율 높음(cn7 11/17건, rg3 25/25건). cn7에는 시간 드리프트로 인한 누수 위험(Mold_Temperature AUC 0.89는 시간 누수성). unlabeled 비가동 고정값 블록 큼(cn7 51.5%, rg3 36.6%). → GroupKFold(X-키) + 시간 블록 분할 병행 필요.
  - **③ 프레스 유압펌프** — 품질 진단 완료. 이상 이벤트 1건뿐(outlier 세그먼트 21개)이라 지도학습 일반화 주장 어려움. 전류 신호 60Hz→10Hz 에일리어싱(FFT 무의미). 라벨 노이즈 의심 세그먼트 존재. → 정상 데이터 기반 이상탐지 + 세그먼트 단위 GroupKFold + 탐지 지연/오경보율 지표 권장.
  - **④ X-ray** — 미착수. 착수 시 해상도·비트심도·어노테이션 형식(bbox/mask)·클래스 불균형부터 확인.
- 주제 확정 후 할 일: 이 섹션을 확정 주제 하나로 정리, 심사 기준표 각 항목에 무엇을 채울지 구체 계획(누가/언제) 추가.
