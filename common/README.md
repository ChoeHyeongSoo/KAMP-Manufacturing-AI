# common/ — 주제 공통 유틸

주제가 확정되면 아래를 여기로 올린다. 그 전까지는 각 `topics/<주제>/src/`에 둔다.

- `io.py` — zip 해제, 인코딩 감지, 원본 인덱스 처리
- `split.py` — GroupKFold / 시간 블록 분할 (누수 방지)
- `metrics.py` — F1·PR-AUC·확률보정 지표, 세그먼트 단위 탐지율·오경보율
- `plot.py` — 한글 폰트 설정, 저장 헬퍼
